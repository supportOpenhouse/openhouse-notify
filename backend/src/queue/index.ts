import { logger } from '@infrastructure/logger';

logger.info('Queue system entry point loaded');

// Queue instances exported for use across the application
// Placeholder: queue registrations pending

export {};
